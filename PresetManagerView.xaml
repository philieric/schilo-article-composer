<UserControl x:Class="SchiloArticleComposer.PresetManagerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <UserControl.Resources>
        <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Margin" Value="0,0,8,0" />
            <Setter Property="Background" Value="{DynamicResource SchiloButtonBackgroundBrush}" />
            <Setter Property="BorderBrush" Value="{DynamicResource SchiloButtonBorderBrush}" />
            <Setter Property="BorderThickness" Value="{DynamicResource SchiloButtonBorderThickness}" />
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Modeles d'instructions" FontSize="16" FontWeight="Bold" Margin="0,0,0,10" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="280" />
                <ColumnDefinition Width="10" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <!-- Liste des modeles -->
            <DockPanel Grid.Column="0">
                <Button x:Name="NewPresetButton" DockPanel.Dock="Bottom" Content="Nouveau modele..."
                        Click="NewPresetButton_Click" HorizontalAlignment="Stretch" Margin="0,10,0,0" />
                <ListBox x:Name="PresetListBox" SelectionChanged="PresetListBox_SelectionChanged"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding}" TextWrapping="Wrap" Padding="2" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </DockPanel>

            <!-- Detail du modele selectionne -->
            <DockPanel Grid.Column="2">
                <TextBlock x:Name="SelectedNameText" DockPanel.Dock="Top" FontWeight="Bold" FontSize="14" Margin="0,0,0,8" />
                <TextBlock x:Name="DefaultHintText" DockPanel.Dock="Top" Foreground="#0067c0" Margin="0,0,0,8" TextWrapping="Wrap" />

                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" Margin="0,10,0,0">
                    <Button x:Name="SaveButton" Content="Enregistrer les modifications" Click="SaveButton_Click" />
                    <Button x:Name="SetDefaultButton" Content="★ Definir par defaut" Click="SetDefaultButton_Click" />
                    <Button x:Name="DeleteButton" Content="Supprimer" Click="DeleteButton_Click" />
                </StackPanel>

                <TextBlock DockPanel.Dock="Top" Text="Instructions pour l'IA :" FontWeight="Bold" Margin="0,0,0,4" />
                <TextBox x:Name="InstructionsBox" AcceptsReturn="True" TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto" Padding="6" />
            </DockPanel>
        </Grid>

        <TextBlock x:Name="StatusText" Grid.Row="2" Foreground="#4CAF50" TextWrapping="Wrap" Margin="0,10,0,0" />
    </Grid>
</UserControl>
